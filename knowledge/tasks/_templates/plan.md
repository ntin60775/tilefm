# План задачи TASK-2026-0001

## Правило

Для задачи существует только один файл плана: `plan.md`.
Если задача декомпозируется, каждая подзадача получает свой собственный `plan.md` внутри своей папки.

## Паспорт плана

| Поле | Значение |
|------|----------|
| ID задачи | `TASK-2026-0001` |
| Parent ID | `—` |
| Версия плана | `1` |
| Связь с SDD | `не требуется` |
| Дата обновления | `YYYY-MM-DD` |

## Цель

Кратко описать ожидаемый результат этой задачи или подзадачи.

## Границы

### Входит

- ...

### Не входит

- ...

## Планируемые изменения

### Код

- ...

### Конфигурация / схема данных / именуемые сущности

- ...

### Документация

- ...

## Зависимости и границы

### Новые runtime/package зависимости

- `нет`

### Изменения import/module-связей и зависимостей между модулями

- `нет`

### Границы, которые должны остаться изолированными

- ...

### Критический функционал

- ...

### Основной сценарий

- ...

### Исходный наблюдаемый симптом

- Если задача поднята по инциденту, логам или деградации, перечислить проверяемые маркеры, сообщения, условия воспроизведения или диагностические признаки.
- Иначе явно указать `не требуется`.

## Риски и зависимости

- ...

## Связь с SDD

- если для задачи нужен `sdd.md`, здесь перечислить только этапы и контрольные точки, которые план реализует;
- если для задачи нужен `sdd.md`, здесь же сослаться на `artifacts/verification-matrix.md` и контрольные точки покрытия матрицы;
- если задача вводит или меняет первичный нормативный contract, здесь явно назвать канонический файл и перечислить только производные документы, которые должны быть синхронизированы;
- не дублировать целиком Problem, Architecture, Acceptance Criteria и No-Go из SDD;
- если SDD не нужен, явно оставить пометку `не требуется`.

## Проверки

### Что можно проверить кодом или тестами

- Сначала перечислить максимально полный набор автоматических проверок по backend, frontend, интеграциям и другим затронутым контурам, который реально исполним в проекте и допустим его правилами.
- Для задачи с `sdd.md` отдельно указать, какими командами или тест-наборами доказывается покрытие `artifacts/verification-matrix.md`.
- `git diff --check`
- синхронизацию поля `Ветка` с `task.md` и `registry.md`
- ...

### Что остаётся на ручную проверку

- Оставлять только непокрытый автоматикой остаток, который нельзя закрыть без нарушения правил и ограничений проекта.
- Итоговый консолидированный список ручных проверок вести в `task.md` общей задачи, а не размазывать по нескольким файлам.
- ...

## Шаги

- [ ] Шаг 1
- [ ] Шаг 2
- [ ] Шаг 3

## Критерии завершения

- ...
