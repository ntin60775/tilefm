# Карточка задачи TASK-2026-0001

## Паспорт

| Поле | Значение |
|------|----------|
| ID задачи | `TASK-2026-0001` |
| Parent ID | `—` |
| Уровень вложенности | `0` |
| Ключ в путях | `TASK-2026-0001` |
| Технический ключ для новых именуемых сущностей | `—` |
| Краткое имя | `zapolnit-korotkim-slug` |
| Человекочитаемое описание | Короткое описание задачи для CLI, registry и ежедневного статуса |
| Статус | `черновик` |
| Приоритет | `средний` |
| Ответственный | `не назначен` |
| Ветка | `task/task-2026-0001-zapolnit-korotkim-slug` |
| Требуется SDD | `нет` |
| Статус SDD | `не требуется` |
| Ссылка на SDD | `—` |
| Дата создания | `YYYY-MM-DD` |
| Дата обновления | `YYYY-MM-DD` |

## Цель

Кратко описать, какую проблему решает задача и какой результат нужен пользователю.

## Подсказка по статусу

Использовать только одно из значений:

- `черновик`
- `готова к работе`
- `в работе`
- `на проверке`
- `ждёт пользователя`
- `заблокирована`
- `завершена`
- `отменена`

## Git-подсказка

- Поле `Ветка` хранит текущую активную ветку рабочего контекста, а не обязательную долгоживущую task-ветку.
- При открытии верхнеуровневой задачи стартовый контекст обычно синхронизируется в `task/<task-id-lower>-<slug>`.
- Для первой и последующих поставок helper может переводить `Ветка` в delivery-ветку вида `du/<task-id-lower>-uNN-<slug>`.
- Для подзадачи по умолчанию можно указывать ветку родителя, если отдельная ветка или delivery unit не нужны.
- При каждом создании или переключении ветки нужно синхронизировать это поле и строку в `registry.md`.

## Границы

### Входит

- ...

### Не входит

- ...

## Контекст

- источник постановки: ...
- связанная бизнес-область: ...
- ограничения и зависимости: ...
- исходный наблюдаемый симптом / лог-маркер: `не требуется`
- основной контекст сессии: `текущая задача`

## Затронутые области

| Область | Что меняется |
|---------|--------------|
| Код / сервисы | ... |
| Конфигурация / схема данных / именуемые сущности | ... |
| Интерфейсы / формы / страницы | ... |
| Интеграции / обмены | ... |
| Документация | ... |

## Связанные материалы

- основной каталог задачи: `knowledge/tasks/TASK-2026-0001-zapolnit-korotkim-slug/`
- файл плана: `plan.md`
- файл SDD: `sdd.md`, если он обязателен для этой задачи
- файл verification matrix: `artifacts/verification-matrix.md`, если для задачи обязателен `sdd.md`
- канонический нормативный contract или source-of-truth для доменной модели, если задача его меняет: ...
- пользовательские материалы: ...
- связанные коммиты / PR / ветки: перечислять delivery-ветки, PR/MR и merge-коммиты по мере появления
- связанные операции в `knowledge/operations/`, если есть: ...

## Контур публикации

Delivery unit описывает конкретную поставку через ветку и публикацию.
В одном `task.md` допускается `0..N` delivery units.
`registry.md` хранит только задачи и подзадачи, поэтому delivery units в него не добавляются.

| Unit ID | Назначение | Head | Base | Host | Тип публикации | Статус | URL | Merge commit | Cleanup |
|---------|------------|------|------|------|----------------|--------|-----|--------------|---------|
| `—` | — | `—` | `—` | `none` | `none` | `planned` | `—` | `—` | `не требуется` |

Подсказка по статусам delivery unit:

- `planned` — контур поставки ещё не стартовал.
- `local` — delivery-ветка создана локально, публикации ещё нет.
- `draft` — draft PR/MR опубликован.
- `review` — публикация готова к review.
- `merged` — поставка влита, merge commit зафиксирован.
- `closed` — поставка закрыта без merge.

## Текущий этап

Коротко зафиксировать, где сейчас находится задача: анализ, реализация, проверка, ожидание пользователя, пауза.

## Стратегия проверки

### Покрывается кодом или тестами

- Перечислить только тот максимально полный набор автоматических проверок по всем затронутым контурам, который реально исполним в текущем проекте и допускается его правилами.
- Не включать сюда проверки, которые теоретически возможны, но запрещены или ограничены правилами проекта, среды, доступов или данных.
- Для каждого пункта указывать точную команду, тест-набор, скрипт или другой воспроизводимый способ проверки.
- Если задача требует `sdd.md`, отдельно перечислить команды или тесты, которыми доказано покрытие `artifacts/verification-matrix.md`.
- ...

### Остаётся на ручную проверку

- Оставлять здесь только то, что нельзя закрыть допустимой для проекта воспроизводимой автопроверкой.
- Если это подзадача, не делать этот список конкурирующим итоговым checklist: пункты должны быть перенесены в родительскую общую задачу.
- ...

## Критерии готовности

- ...

## Итоговый список ручных проверок

Для общей задачи здесь ведётся единый консолидированный список ручных сценариев, оставшихся после автоматизации.

- Если это общая задача, перечислять здесь финальные ручные проверки списком.
- Если это подзадача, не дублировать общий итоговый checklist; вместо этого передать пункты в родительскую задачу и при необходимости сослаться на неё.
- Если ручных проверок не осталось, явно написать `не требуется`.

## Итог

Заполняется при завершении или передаче: что фактически сделано, что осталось, какие есть риски.
